
@{
    ViewBag.Title = "Login";
}

<h2>Login</h2>
<table>
    @using (Html.BeginForm())
    {
        <tr>
            <td>ten dang nhap</td>
            <td>@Html.TextBox("ma")</td>
        </tr>
        <tr>
            <td>mat khau</td>
            <td>@Html.TextBox("mk")</td>
        </tr>
        <tr>
            <td colspan="2">
                <input type="submit" value="Login" />
            </td>
        </tr>
    }
</table>
@if(ViewBag.error != null)
            {
                <div>@ViewBag.error</div>
            }

